{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Deliberately no "DOM" and no @types/node: core must stay runnable in browser, Node, and workers.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts", "src/testing/**"]
}
